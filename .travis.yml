language: cpp
addons:
  apt:
    sources:
      - george-edison55-precise-backports # CMake 3.2
      - ubuntu-toolchain-r-test
    packages:
      - cmake-data
      - cmake
      - gcc-5
      - g++-5

env:
  - COMPONENT=game-server
  # TODO configure ci for web server component

script: ci/script.sh
# TODO set up tests and run them in script.sh

notifications:
  email: false
